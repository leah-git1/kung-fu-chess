services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: kfc
      POSTGRES_PASSWORD: kfc
      POSTGRES_DB: kfc
    ports:
      - "5432:5432"
    volumes:
      - kfc-pg:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  nats:
    image: nats:2-alpine
    ports:
      - "4222:4222"

  # ── shared backend image (auth, rooms-api, rating) ────────────────────────
  auth-service:
    build:
      context: .
      dockerfile: services/backend/Dockerfile
    image: kfc-backend
    command: uvicorn services.backend.auth.main:app --host 0.0.0.0 --port 8000
    expose:
      - "8000"
    environment:
      - DATABASE_URL=postgresql://kfc:kfc@postgres:5432/kfc
    depends_on:
      - postgres

  rooms-api:
    image: kfc-backend
    command: uvicorn services.backend.rooms_api.main:app --host 0.0.0.0 --port 8001
    expose:
      - "8001"
    environment:
      - REDIS_HOST=redis
    depends_on:
      - auth-service   # ensures kfc-backend image is built first
      - redis

  rating-service:
    image: kfc-backend
    command: uvicorn services.backend.rating.main:app --host 0.0.0.0 --port 8002
    expose:
      - "8002"
    environment:
      - DATABASE_URL=postgresql://kfc:kfc@postgres:5432/kfc
    depends_on:
      - auth-service   # ensures kfc-backend image is built first
      - postgres

  # ── dedicated images ──────────────────────────────────────────────────────
  matchmaker:
    build:
      context: .
      dockerfile: services/matchmaker/Dockerfile
    expose:
      - "8003"
    environment:
      - REDIS_HOST=redis
      - NATS_URL=nats://nats:4222
    depends_on:
      - redis
      - nats

  game-allocator:
    build:
      context: .
      dockerfile: services/game-allocator/Dockerfile
    expose:
      - "8004"
    environment:
      - REDIS_HOST=redis
      - NATS_URL=nats://nats:4222
      - ROOMS_HOST=rooms-api
      - ROOMS_PORT=8001
      - SHARD_PUBLIC_HOST=localhost
    depends_on:
      - redis
      - nats
      - rooms-api
      - game-shard

  game-shard:
    build:
      context: .
      dockerfile: services/game-shard/Dockerfile
    ports:
      - "5556-5559:5556-5559"
    environment:
      - REDIS_HOST=redis
      - RATING_HOST=rating-service
      - RATING_PORT=8002
      - SHARD_PORT=5556
      - SHARD_HOST=game-shard
      - NUM_WORKERS=2
    depends_on:
      - redis
      - rating-service

  api-gateway:
    build:
      context: .
      dockerfile: services/api-gateway/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - AUTH_HOST=auth-service
      - AUTH_PORT=8000
      - REDIS_HOST=redis
    depends_on:
      - auth-service
      - redis

  ws-gateway:
    build:
      context: .
      dockerfile: services/ws-gateway/Dockerfile
    ports:
      - "5555:5555"
    environment:
      - REDIS_HOST=redis
      - NATS_URL=nats://nats:4222
      - MM_HOST=matchmaker
      - MM_PORT=8003
      - ROOMS_HOST=rooms-api
      - ROOMS_PORT=8001
      - ALLOCATOR_HOST=game-allocator
      - ALLOCATOR_PORT=8004
      - WS_PORT=5555
    depends_on:
      - redis
      - nats
      - api-gateway
      - rooms-api
      - matchmaker
      - game-allocator
      - game-shard

volumes:
  kfc-pg:
